@extends('core-cms::shared.blocks.layouts.layout')@php    $block = $block ??  [];    $useContainer = $useContainer ?? $block['use-container'] ?? true;    $section = $section ?? 'section';    $classes = ['card'];    $imageHeight = 75;    if(key_exists('media-height', $block) && $block['media-height'] !== "") {        $imageHeight = $block['media-height'];    }@endphp@section('class')    {{ join(' ', $classes) }}@overwrite@section('element')    {{$section}}@overwrite@section('content')    @php        $cardClasses = [];        if($useContainer) {            $cardClasses[] = 'container';        }    @endphp    <div class="{{ join(" ", $cardClasses) }}">        @if(key_exists('media', $block) && $block['media'] !== "")            <div class="margin-block-end-6 text-center">                {!! image_tag($block['media'], key_exists('media-alt', $block) ? $block['media-alt'] : null, $imageHeight) !!}            </div>        @endif        @if(key_exists('title', $block)  && $block['title'] !== "")            @include('core-cms::shared.blocks.components.title', ['block' => $block])        @endif        @if(key_exists('content', $block)  && $block['content'] !== "")            @include('core-cms::shared.blocks.components.content', ['block' => $block])        @endif        @if(key_exists('ctas', $block) && count($block['ctas']) > 0)            <div class="flex-group align-items-center">                @foreach($block['ctas'] as $cta)                    @include('core-cms::shared.blocks.components.cta', ['block' => $cta])                @endforeach            </div>        @endif    </div>@overwrite